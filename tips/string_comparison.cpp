#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main()
{

  string curr = "59";
  string target = "60";

  cout << "(curr < target):" << (curr < target) << endl;
  cout << "(curr > target):" << (curr > target) << endl;
}